apiVersion: v1
kind: Service
metadata:
  name: internal-ai-router
  labels:
    app: internal-ai-router
spec:
  type: ClusterIP
  selector:
    app: internal-ai-router
  ports:
    - name: http
      protocol: TCP
      port: 8787
      targetPort: http
