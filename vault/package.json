{
  "name": "ai-shopify-superapp-vault",
  "version": "0.1.0",
  "private": true,
  "description": "Gadget vault app for Shopify return sync, theme profiles, and webhook ledger",
  "license": "UNLICENSED",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "gadget-server": "link:.gadget/server"
  },
  "devDependencies": {
    "@types/node": "^20.8.4",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
