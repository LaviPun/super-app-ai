# Design-System Audit — 2026-07-10

Scope: `Module Design System.md` + `Template Gallery.dc.html` verified against the live repo (`ai-shopify-superapp`) — schema, compiler, renderer, blocks, CSS.

## ✅ Verified correct (no action needed)

1. **Token grammar (§2) = code, exactly.** Every enum in the doc matches `packages/core/src/allowed-values.ts` / `storefront-style.ts` verbatim: layout modes/anchors/widths/z-index, spacing, typography, radius/border/shadow/elevation, density, motion durations/easings, packs (`auto·luxe·bold`), `customCss ≤ 2000`, offsets ±100, scaling 50–150.
2. **§5 vocabulary = code.** Popup triggers/frequency/showOnPages, all 10 recommendation strategies + the static/dynamic split + fallbacks + limits (1–12), pricing/discount/threshold enums — all exact.
3. **Pipeline binding (§9.1) is real.** `style-packs.server.ts`, `style-compiler.ts`, `design-qa.server.ts`, `apply-style-pack.server.ts` all exist with tests (`design-system.test.ts`, `style-compiler.test.ts`, `apply-style-pack.test.ts`, `oklch-ramp.test.ts`).
4. **The PATCH is merged.** Live `superapp-module.liquid` resolves `sa_pack` (block setting → `style_json.pack` → `luxe` fallback) and wraps everything in `.superapp-scope[data-sa-pack]`. All 4 blocks pass the params.
5. **Runtime CSS carries the full §9.3 token map** — both pack maps, reduced-motion branches, 2px focus-visible rings, 44px min-heights, OKLCH ramp hookup.
6. **Gallery coverage:** §01–§09 all present; all 8 module kinds + 13 archetypes in both packs; F1–F8 all shown; interactions index = 8 × 10 = 80 ✓.
7. **Conversion patterns present in code:** free-shipping progress band, countdown band, upsell/bought-together, low-stock/price-flash/cart-shake micro-accents, social-proof popups, skeleton loading — good high-conversion coverage.

## 🔧 Fixed in this pass

1. **Doc drift — renderer ahead of doc.** Live renderer dispatches 4 archetypes the doc didn't list: `cta`, `upsell`, `band` (announcement/countdown/free-ship), `technical` (config-summary card). Added to §4.2. Also split `contact · team · timeline` (three branches, not one) and extended §5.2 block kinds (`faq-item · plan · review-card · team-member · contact-method · milestone · event · logo · badge · product-card/product/addon`).
2. **`pdp` flagged:** doc listed it as a dedicated archetype; the renderer had **no `pdp` branch** — it fell through to the generic section. Noted in §4.2.
3. **Stale doc copy:** `docs/design-system/module-design-system.md` was 2 decision-log entries behind the root doc. Synced.
4. **Stale extension copy:** the project's `extensions/theme-app-extension/` was behind the repo — repo blocks now ship the full layer-5 override set (`override_radius/density/align/text/bg`), the patched snippet, `superapp-modules.js`, and `superapp-theme-head.liquid`. Synced all files from the repo; deleted the obsolete `PATCH-superapp-module.md` (patch is merged).
5. **Layer-5 override set documented** in §5.2 (was only implied in §3.3.3).

## ✅ Follow-ups — closed 2026-07-10 (same day)

1. ~~Gallery §06 lacks the 4 new archetypes~~ — **Done.** `cta`, `upsell`, `band` (countdown + free-ship progress), `technical`, and `sticky-atc` previews added in both packs (§06 now 8 kinds + 18 archetypes).
2. ~~Effects index missing `shooting-stars`~~ — **Done** (index now 45).
3. ~~Interactions index missing `carousel`~~ — **Done** (Navigation group; index now 81).
4. ~~No `pdp` renderer branch~~ — **Done.** Dedicated branch built (gallery + thumbs + buy box: eyebrow / title / rating / price + compare-at / variant pills / ATC / trust line) + `.superapp-pdp` CSS in both packs. Aliases: `pdp · product-page · product-detail · buy-box`.
5. ~~No sticky add-to-cart bar~~ — **Done.** New `sticky-atc` archetype (moved out of the `technical` alias list): fixed viewport-bottom bar, safe-area aware, 44px ATC, `.superapp-satc` CSS in both packs + reduced-motion branch. Documented in §4.2.
6. ~~Copy the updated `Module Design System.md`, `superapp-module.liquid`, and `superapp-modules.css` back into the repo~~ — **Done 2026-07-10 (repo side).** Doc merged (repo-only §2.7/§3.3.4/§9.4 sections preserved); `pdp` + `sticky-atc` branches and `.superapp-pdp`/`.superapp-satc` CSS merged surgically — the project copies' stale `var(--sa-ink)` CTA fallbacks were NOT taken (repo's `CanvasText` fix is newer, contract-tested), and the repo-only `'story'`/`'badge'` aliases were kept.
