# Drift Ledger

> Rows close via a dated audit pass (see docs/audit/README.md) — strikethrough + **RESOLVED** + commit SHA, never deleted.

| Claim                                                | Reality                                                                                                | Decision                                                                                 | PR  |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | --- |
| ~~Lint quality is tightly enforced.~~ **RESOLVED** | Superseded, not stale as originally framed: `apps/web/package.json`'s `lint` script already caps at `--max-warnings 100`, not `250` — the ratchet this row asked for already happened. Verified directly against the file (`"lint": "eslint app scripts --format unix --max-warnings 100"`), not against a prior claim. | No action needed — the budget was already ratcheted down. | `0285bca` |
| ~~Eval quality gate matches SLO.~~ **RESOLVED** | Superseded, not stale as originally framed: `ci.yml`'s `evals` job already runs `pnpm evals --strict` with `EVAL_THRESHOLD_SCHEMA`/`EVAL_THRESHOLD_COMPILER` explicitly set to `0.99` (matching the SLO target this row asked CI to be raised to). The `0.9` figure is only the *default* threshold `run-evals.ts` uses when `--strict` isn't passed and no env override is set — not what CI actually gates on. See `docs/testing.md` §4 for the full default-vs-strict breakdown. | No action needed — CI already gates at 0.99, matching the SLO. | `0285bca` |
| Phase 7 rate limiting is hard enforcement.           | Current limiter is in-memory only (`apps/web/app/services/security/rate-limit.server.ts`).             | Add Redis-backed limiter behind shared interface; keep in-memory for local/test only.    | TBD |
| ~~Billing mode is environment-safe.~~ **RESOLVED**   | Superseded, not fixed as originally framed: `BillingService.createSubscription()` / `appSubscriptionCreate` / `BILLING_TEST_MODE` were removed entirely (commit `132a82a`). The app no longer creates in-app charges — merchants approve a plan on Shopify's hosted App Pricing page and `PlanSyncService.syncShop()` reconciles the plan of record from the Partner API. The `NODE_ENV`-coupled test-billing path this row warned about no longer exists. | No action needed — the risky code path was deleted, not patched. | `132a82a` |
| ~~Persistence layer is production-grade.~~ **RESOLVED** | Superseded: `apps/web/prisma/schema.prisma`'s `datasource` block is `provider = "postgresql"` as of the WS-A Railway cutover (comment cites "2026-08-24 Railway cutover (WS-A)"; see `docs/runbooks/postgres-migration.md`). The SQLite-only claim this row warned about is no longer true. | No action needed — the cutover shipped. | `1e67530` / PR `1b2b5e2` |
| Agent API endpoint count claim is accurate.          | Docs claim 28 endpoints; discovery/index evidence reports 30.                                          | Reconcile endpoint counts and source-of-truth docs.                                      | TBD |
| Test count claim is consistent across docs.          | Conflicting counts: 163 vs 253 appear across docs.                                                     | Reconcile all published counts to one canonical value.                                   | TBD |
| Phase 2 theme engine scope is complete.              | Only `theme-analyzer.service.ts` exists in `services/theme/`; docs imply broader scope.                | Either ship missing theme engine pieces or narrow docs to shipped scope.                 | TBD |
| Publish preflight is fully comprehensive.            | Current preflight checks are narrower than broad readiness wording.                                    | Expand checks or narrow wording to exact guard coverage.                                 | TBD |
| ~~Workflow connectors are production-ready.~~ **RESOLVED** | Superseded, not stale as originally framed: `apps/web/app/services/workflows/connectors/email.connector.ts:94-206` does a real HTTP `fetch` to SendGrid (`DEFAULT_SENDGRID_API_URL`) or a configured generic provider (`EMAIL_API_URL`) — building the provider-specific payload, handling `429`/`5xx`/timeout/network errors with typed `connectorError` results, and parsing the returned message id. It is not a stub; the "Stub implementation" comment this row's claim traced to (present in `bf1f66b`, 2026-03-04) was removed when the connector was rewritten (`0285bca`, 2026-05-15). Callers also fail loudly rather than faking success: `invoke()` returns an `AUTH` error when no API key is present, and `flow-runner.service.ts` documents (and `3ca5ca7` implements) failing loudly when `EMAIL_API_KEY` isn't configured. | No action needed — the connector is a real provider integration, not a stub. | `0285bca` |
| Embedding classifier caching is production-ready.    | Cache is in-process only and called out as needing shared backing.                                     | Move to Redis/DB-backed cache for multi-instance consistency.                            | TBD |
| ~~AI provider fallback behavior is clearly documented.~~ **RESOLVED** | `docs/ai-providers.md` "Common pitfalls" now states plainly that `StubLlmClient` (`llm.server.ts:122`) is only constructed by `evals.server.ts`/`tournament.server.ts`, and that `getLlmClient()`'s production path throws `AiProviderNotConfiguredError` when unconfigured — no stub/placeholder fallback in production. | No action needed — doc clarified. | (WS-J Task 5 commit) |

### Post-ship sync pass, 2026-09-02 (PRs #33–#48 delta)

Rows found and fixed in the same docs-only PR (`docs/post-ship-sync`), so they are recorded already-resolved:

| Claim                                                | Reality                                                                                                | Decision                                                                                 | PR  |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | --- |
| ~~Metafield refs-lists live under dotted `superapp.*` namespaces.~~ **RESOLVED** | Shopify rejects dots in metafield namespaces — every dotted write had always failed. PR #45 (`97e0622`) renamed all seven namespaces to underscore forms (`superapp_theme` etc.) across writers and readers. Docs (`publishing.md`, `implementation-status.md`, `flows.md`, `generation.md`, `shopify-dev-setup.md`, `runbooks/publish-live-probe.md`) still showed the dotted forms. | Docs updated to underscore forms; rename + related #34 access fix recorded in `publishing.md` §1 and `debug.md` §24. | `97e0622` / docs sync |
| ~~Railway "Wait for CI" deploy gate is not enabled (runbook STATUS + operations.md correction).~~ **RESOLVED** | The owner ran the enable step on 2026-09-02 after the #46 audit; `railway status` reports `checkSuites` enabled on `web` + `worker`, and the `PROD_BASE_URL` repo variable is set (verified via `gh variable list`), making the post-deploy smoke workflow live. | `operations.md` §2 and `runbooks/deploy-and-rollback.md` STATUS updated; added the "Deploy skipped by the CI gate" caveat (SKIPPED deployments are not revived by a green CI re-run — issue #49 for `413d376`). | docs sync |
| ~~AI Providers page only ever shows a masked API key.~~ **RESOLVED** | PR #33 (`4bcff4f`) added a prefix+last-4 preview with an on-demand, audited (`AI_PROVIDER_KEY_REVEALED`) Show/Hide reveal. | `internal-admin.md` updated. | `4bcff4f` / docs sync |
| ~~Eval harness has two modes; `EVAL_PROVIDER_ID` simply switches to a live run.~~ **RESOLVED** | PR #42 (`936e54e`): `EVAL_PROVIDER_ID` pins a DB provider by id/name and fails fast if absent; `env` selects the env client explicitly; eval runs disable fallback and ignore `ANTHROPIC_SKILLS` env. | `testing.md` §4 rewritten for the three modes + isolation; cross-ref in `ai-providers.md`. | `936e54e` / docs sync |
| ~~Anthropic code execution uses the `code_execution_20250825` tool type.~~ **RESOLVED** | PR #43 (`de7b41f`) fixed the client to `code_execution_20260521`; `ai-providers.md` still documented the stale type. | Doc corrected; caching/cache-stats/tool-name-invariance notes added alongside. | `de7b41f` / docs sync |
| ~~`theme.section`/`proxy.widget` recipe budgets and the 0.5× delta ratio are current.~~ **RESOLVED** | PR #41 raised the per-type budgets to 7000/5500; PR #38 raised the delta ratio to 0.75× with a single 1.5× retry on truncation. No doc stated the old numbers as normative, but `ai-providers.md`'s token-limit section omitted per-type budgets entirely. | Added per-type + delta budget subsections to `ai-providers.md`, pointing at `token-budget.server.ts` as source of truth. | `e56f3ca`/`2250d58` / docs sync |
