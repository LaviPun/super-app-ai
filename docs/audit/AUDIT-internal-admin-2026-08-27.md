# internal-admin.md Audit — 2026-08-27

Scope: `docs/internal-admin.md` (WS-J Task 9 — audit + refresh, not a
ground-up rewrite, per the task brief's own framing: "Relatively fresh...
audit pass, not ground-up rewrite") verified against the live repo
(`ai-shopify-superapp`) at `master@c201150` (branch `docs/ws-j-rewrite`,
worktree HEAD at time of writing `be07d86`).

Source-of-truth checklist used (from the task brief): `apps/web/app/routes/internal.*`
(route inventory), `apps/web/app/services/support/*`, `apps/web/app/routes/internal.ai-providers.tsx`,
the SSO allowlist implementation (found via `git log --grep="allowlist"`).

## ✅ Verified correct (no action needed)

1. **Ports, Node version, environment setup** (§Ports) — unchanged, no code drift found in this area.
2. **AI Assistant runtime-target mechanics** (health/chat probe distinction, router-vs-inference, `internal-ai-router.ts` passthrough, cloud checklist, operator banners, session archive/delete, tool audit retention, chat-stream SSE behavior, import-session dedupe) — all spot-checked against the cited files (`internal.ai-assistant.tsx`, `internal-ai-audit-retention.job.ts`, `internal.ai-assistant.chat.stream.tsx`, `internal-assistant-store.server.ts`) and still accurate; no changes made to this content.
3. **Backend validation, Provider HTTP clients, Model pricing, Dashboard sections** — accurate as written, no drift found.
4. **Vendored page-kit, not Polaris.** Confirmed by repo search: zero `@shopify/polaris` imports under `apps/web/app/routes/internal.*` or `apps/web/app/internal-admin/`; `internal.release-dashboard.tsx` (a route not previously in this doc) imports from `~/components/admin/page-kit`, consistent with the rest of the shell.

## 🔧 Fixed in this pass

1. **Theme was undocumented.** Added an explicit "light-only, by design" note citing `apps/web/app/styles/superapp/shell.css:62-66`'s comment ("owner decision 2026-07-14 — no dark theme") and its `color-scheme: light` pin on `.internal-admin-viewport`/`.cmdk`. Confirms the 2026-07-14 revert memory is still true.
2. **SSO allowlist gap was undocumented, and the old text described the pre-fix behavior as if it were still current** (the doc said only "Supports OIDC SSO... via `INTERNAL_SSO_*` env vars" with no allowlist mention — read literally, that implies any authenticated IdP identity gets in). Found the actual fix via `git log --grep="allowlist" -i` → commit `bbdab2a` ("internal SSO callback enforces INTERNAL_SSO_ALLOWED_EMAILS allowlist"). Verified `apps/web/app/internal-admin/sso-allowlist.server.ts`: `evaluateSsoIdentity` requires a present + (when sent) verified email claim on the allowlist, and an **empty/unset allowlist denies every identity** (`reason: 'allowlist_empty'`) rather than falling back to "any identity" — fail-closed, not fail-open. Added a new bullet under Security with this detail and the historical framing (what the gap used to be, cited as historical, never as current behavior — confirmed by grep per the task's verify step).
3. **Merchant module generation provider list was stale/inconsistent with itself.** The "AI provider management" section said merchant `RecipeSpec` generation uses "OpenAI and Anthropic (Claude) only," while this doc's own Features list (line 28) already said "OpenAI / Anthropic / Google Gemini / Azure OpenAI / Custom" are configurable, and `docs/ai-providers.md`'s already-audited product-split table (WS-J Task 5) lists Gemini for merchant generation too. Corrected to match the audited source of truth and flagged the correction inline.
4. **`/internal/ai-providers` "only writer" claim needed a real nuance, not a blanket assertion.** Grepped every `aiProvider.create/update/upsert/delete` call site: `ai-provider.service.ts` (the route's own service — core config) is the only writer of a provider's name/type/API key/base URL/model/active state. Two other real write paths exist but only touch `AiProvider.extraConfig` (JSON metadata: OpenRouter model-catalog sync, billing/dashboard snapshot) and one (`ai-usage.service.ts`) lazy-creates inactive, keyless synthetic rows to satisfy a foreign key for env-key-sourced usage — never a human-reviewed config. Added this as an explicit paragraph rather than leaving an unqualified "only writer" claim that a reviewer's grep could contradict.
5. **Internal AI Assistant's app-awareness was completely undocumented.** `searchAppDocs` (full-text search over this very `docs/` directory, honest `available: false` degrade when absent) and `getAppOverview` (a live, parallel-query DB snapshot: shop/module/flow/connector/data-store counts, active provider, DLQ count, 7-day webhook failure count, 30-day AI usage) are both real and tested (`internal-assistant-search-docs-tool.test.ts`, `internal-assistant-app-aware-routing.test.ts`) per the task brief's explicit ask. Added a new subsection under "Internal AI Assistant setup" describing both, read directly from `internal-assistant-tools.server.ts`'s `runAssistantTool` branches.
6. **Support CRM was entirely undocumented** despite being fully committed (per the `local-triage-llm` memory's claim, which this pass verified rather than trusted at face value). Confirmed real via `apps/web/app/services/support/{triage,autofix,notifications,shopper-intake,ticket-events}.server.ts` and the `internal.support.*` / `support.*` / `api.support.*` routes. Added a full new "Support CRM" section plus a Features bullet and Routes-table rows.
7. **Route table had five real routes missing:** `/internal/model-setup` (referenced extensively in prose throughout this doc but never listed in the Routes table), `/internal/ai-accounts` (a redirect to `ai-providers?tab=accounts`, not a real page), `/internal/release-dashboard` (publish release-safety metrics — added with an explicit naming-collision note, since the left-nav labels it "Release Gate" under **AI & Models** even though it's unrelated to the AI-generation release gate documented in `docs/ai-providers.md`), `/internal/metaobject-backfill` (one-off operator backfill tool), and the three Support CRM routes. Confirmed the nav-label→route mapping via `CommandPalette.tsx`'s `ADMIN_SPECIAL` hash-route table (`#/admin/release` → `/internal/release-dashboard`).
8. **"Logs 4-in-1 nav" (task brief item 1) confirmed and documented.** `internal.tsx`'s `ADMIN_NAV` has one "Logs" entry under **Operations** whose `also` array highlights it for `/internal/api-logs`, `/internal/logs`, and `/internal/audit` in addition to its own `/internal/activity` — four routes, one nav entry, each still its own page. Added a clarifying note so a reader doesn't assume these pages were merged.
9. **Nav section list was stale** ("Overview · Operations · Platform · AI & Models · Catalog" — missing **Support**, which is now its own top-level nav section per `internal.tsx:108-120`). Corrected.

## Follow-ups (open)

1. **`/internal/release-dashboard`'s relationship to the actual publish path (`PublishService`) was not traced in this pass** — it reads `RolloutPolicyService`/`getRecentPublishMetrics`, which may or may not reflect the real `PublishService`/`UnpublishService`/`RollbackService` flow documented in `docs/publishing.md` (which explicitly states there is no progressive-publish/canary mechanism in that real path). This doc only notes the route exists and its naming collision with the AI release gate; whether its underlying metrics are live/meaningful data or a dormant parallel system is out of scope for an internal-admin.md audit and would need its own investigation — possibly relevant to a future `docs/publishing.md` or `docs/operations.md` pass.
2. **`/internal/metaobject-backfill`'s current relevance** (is it still needed, or a one-time migration tool that should be removed) was not assessed — described accurately as what it does, not as a recommendation to keep or remove.

## Drift-ledger rows closed by this pass

None — no existing `drift-ledger.md` row named this doc's stale claims specifically; the corrections above were found and fixed directly rather than pre-tracked.
