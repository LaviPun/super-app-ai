# Post-merge delta pass Audit — 2026-08-27

Scope: WS-J's final delta pass — every "Pending (unmerged)" / "Pending (unmerged PR #N)" marker across the canonical docs (`docs/architecture.md`, `docs/operations.md`, `docs/flows.md`, `docs/internal-admin.md`, `docs/debug.md`, root `CHANGELOG.md`), re-verified against the live repo after wave-two merged to master (`master@eb82ae7` = merge of `origin/master@8a656af` into `docs/ws-j-rewrite`, covering PRs #16–#20, #22–#24). Not a full doc-by-doc re-audit (those already exist as `AUDIT-<slug>-2026-08-27.md` from the initial rewrite pass) — this pass is scoped to the specific passages that were placeholders pending this exact merge.

## ✅ Verified correct (no action needed)

1. **`JOB_EXECUTION_MODE` defaults to `inline`.** `packages/job-orchestration/src/config.ts:20-22` — `modeRaw === 'queue' || modeRaw === 'disabled' || modeRaw === 'inline' ? modeRaw : 'inline'`, and the schema default is `'inline'`. Confirmed the async engine (WS-C, #19) is a real deploy decision, not a default.
2. **`buildWorkerHandlers()` registers exactly two queues.** `apps/web/app/services/jobs/processors/index.ts:22-62` returns `{ 'ai-generation': dispatchAiGeneration, publish: dispatchPublish }` — `connector`/`flow`/`webhook`/`retention` (declared in `packages/platform-contracts/src/platform-jobs.ts`) have no registered handler.
3. **`ROUTE_ORDER` fix is real.** `apps/web/app/services/flows/flow-runner.service.ts:495-512` has a genuine `ROUTE_ORDER` branch (fulfillment-order lookup then `fulfillmentOrderMove`) and the fallthrough now throws `Unknown flow step kind: <kind>` instead of returning `{ skipped: true }`. Commit `7d68b21` (#20).
4. **`customers/data_request` now delivers.** `apps/web/app/services/gdpr/data-request-export.server.ts` (480 lines) implements `compileCustomerDataExport` (queries `DataCapture`/`DataStoreRecord`/`ModuleEvent`/`AttributionLink`/`SupportTicket`) and `deliverCustomerDataExport` (emails the shop owner via the existing mailer). Commit `e45c061` (#23).
5. **`shop/redact` covers 31 shop-scoped models.** Counted directly from `apps/web/prisma/schema.prisma`: 31 of 51 total models declare a `shopId`/`shop` relation; `shop-redact-completeness.test.ts` asserts every one is either redacted or explicitly retained via static analysis of the redact route.
6. **`/internal/funnel` is real, not aspirational.** `apps/web/app/routes/internal.funnel.tsx` (219 lines) + `services/observability/funnel.service.ts` (130 lines) — `FunnelService.windowStats` computes `classified`/`optioned`/`hydrated`/`published` counts and rates by shared `Job.correlationId`, sourced only from the `Job` table (not `AiUsage`/`ActivityLog` — corrected during drafting).
7. **`OpsAlertService` fan-out is real.** `apps/web/app/services/observability/ops-alert.server.ts` (227 lines) fans out via `Promise.allSettled`; Sentry unconditional, Slack/email gated by a rolling-window `OPS_ALERT_OCCURRED` count plus an `OPS_ALERT_FIRED`-driven cooldown (verified the two-row split avoids the bootstrap-deadlock the commit message describes).
8. **`/internal/integrations` (Integrations Hub) exists and covers UptimeRobot/Healthchecks.io status tiles.** `apps/web/app/routes/internal.integrations.tsx` (961 lines) — confirmed via direct grep this gives both monitors a real in-repo footprint, contradicting `docs/operations.md`'s prior "neither has any in-repo footprint" claim (now fixed in this pass).

## 🔧 Fixed in this pass

1. **`docs/architecture.md`'s `worker` process row** — was `Pending (unmerged): WS-C wires real BullMQ workers into this process`; rewritten to describe the real two-mode behavior (`inline` default / `queue` opt-in) with the actual registered-queue list.
2. **`docs/operations.md`'s `worker` process row** — same fix, plus new subsections for the Integrations Hub, `OpsAlertService`, and `/internal/funnel` (previously undocumented), plus the stale "neither [UptimeRobot/Healthchecks.io] has any in-repo footprint" claim corrected.
3. **`docs/internal-admin.md`'s "DLQ replay — what's real today" section** — corrected the claim that `scripts/worker.ts` "mounts no BullMQ Worker" (now false); clarified the legacy `Job`-table replay path (`CONNECTOR_TEST`/`FLOW_RUN`/`MESSAGING_RUN`/`HTTP_SYNC_RUN`) remains genuinely unconsumed, but for a different reason than an unbuilt worker — it's a separate queue system from the new `PlatformQueueName` BullMQ queues WS-C wired up.
4. **`docs/debug.md` §13, §18 (two spots), §19** — four passages describing WS-C as "not yet merged" / "unmerged as of this writing" rewritten to describe the merged-but-flag-gated reality.
5. **`docs/flows.md` §1 and §5** — the `ROUTE_ORDER` "known gap"/"currently-open gap" language struck through and marked fixed (#20), with the still-true residual caveat (not authorable) preserved. Added the missing `flow-runner-route-order.test.ts` to the §6 Verify command.
6. **Three `docs/flow-automation.md`-path comments in app source** — `workflow-engine.service.ts`, `expression-evaluator.ts`, `services/workflows/connectors/shopify.connector.ts` cited a doc path that no longer exists (WS-J renamed it to `docs/flows.md`); repointed to the new file + section name. Comment-only changes, no behavior change.
7. **`CHANGELOG.md`** — added 8 entries (PRs #16–#20, #22–#24) with real commit SHAs pulled from `git log`; updated the `[Unreleased]` section to drop the now-merged workstreams, leaving only WS-I (still genuinely unmerged as of this pass).
8. **Claude Code project memory** (`~/.claude/projects/-Users-lavipun-Work-ai-shopify-superapp/memory/`, outside this repo) — `MEMORY.md` Task 15 steps executed: no stale doc-path references found (verify grep already empty), `flow-automation-engine.md` bullet trimmed to a pointer, `launch-wave-two-2026-08.md` bullet updated to reflect wave-two completion, new `docs-rewrite-ws-j.md` file + See Also bullet added, Cloudflare-tunnel section flagged (not rewritten, per Task 15 Step 4's explicit scope limit). `flow-automation-engine.md` and `launch-wave-two-2026-08.md` themselves also got small surgical append-only corrections (durable-wait/ROUTE_ORDER reality; PR #13 merged) since they directly and materially contradicted the new merged state.

## Follow-ups (open)

1. **`apps/web/app/components/admin/integration-tiles.ts`'s `healthchecks` tile description** still reads "...GitHub Actions cron workflow (PR #13, not yet merged)..." — PR #13 has since merged (`79472e0`). This is app-source UI copy, out of scope for a docs-only pass; flagged as a separate spawned task rather than fixed here.
2. **Drift-ledger row "Workflow connectors are production-ready... Email connector remains a stub implementation"** looks potentially stale — `EmailConnector` (`apps/web/app/services/workflows/connectors/email.connector.ts`) shows no stub markers on a quick grep, and `docs/flows.md` already describes `SEND_EMAIL_NOTIFICATION` as "real send via the email connector, throws loudly if `EMAIL_API_KEY` is unset." Not independently re-verified in this pass (out of scope — this pass tracked merge-driven "Pending (unmerged)" markers specifically, not the full open drift-ledger backlog); worth a dedicated look in a future audit pass.
3. **`docs/launch/review-notes.md`** still has 3 literal `Pending (unmerged PR)` markers (WS-C/WS-F/WS-G-related). Deliberately left alone — `docs/launch/` is WS-S's (#22) territory, not WS-J's, per this pass's explicit instructions.

## Drift-ledger rows closed by this pass

None — the markers this pass closed were doc-internal "Pending (unmerged)" placeholders written by WS-J itself, not pre-existing `drift-ledger.md` rows.
