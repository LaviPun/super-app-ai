# Shared Facts Glossary (SSOT)

This file is the single source of truth for shared numeric facts referenced across `README.md`, `docs/implementation-status.md`, and `docs/phase-plan.md`.

## Shared numeric facts

- **Automated test baseline:** `347` total tests (`docs/audit/test-baseline.json`).
- **Agent API endpoint count:** `30` endpoints (enumerated in `README.md` under "Agent API").
- **Phase 2 scope label:** `Theme Compatibility Engine v1 (Theme profiling + safe mounting)`.

## Usage rule

When a shared number changes, update this file first, then propagate the change to all docs that cite the fact.
