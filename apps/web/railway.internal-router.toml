# Railway service config — set root directory to repo root in the Railway dashboard.
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/web/Dockerfile.internal-router"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
