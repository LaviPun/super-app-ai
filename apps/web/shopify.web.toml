roles = ["frontend", "backend"]
# Port omitted: CLI assigns a port and sets PORT when running `shopify app dev`.
# When running `pnpm dev` directly, Vite uses PORT from env or defaults to 3000.

[commands]
dev = "pnpm dev"
build = "pnpm build"
