# Checkout UI Extension (generic)

A single checkout UI extension can render different UIs based on config stored in shop metafields
(e.g. `superapp.checkout.upsell`).

Note: Shopify docs state that Checkout UI extensions for Information/Shipping/Payment steps require Shopify Plus.
