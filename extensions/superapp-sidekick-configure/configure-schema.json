{
  "$schema": "https://schemas.shopify.com/admin/intents/2026-04/intent.json",
  "value": {
    "type": "string",
    "description": "The id (gid://application/superapp-module/<id>) of the Super App AI module to configure. Comes from a search_modules result.",
    "mapTo": "param",
    "fieldName": "id"
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "instruction": {
        "type": "string",
        "description": "Optional natural-language change to pre-fill in the module's builder, e.g. 'make the countdown 24 hours' or 'change the discount to 15%'.",
        "mapTo": "query_param",
        "fieldName": "instruction"
      }
    }
  }
}
