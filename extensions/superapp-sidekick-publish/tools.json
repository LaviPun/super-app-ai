[
  {
    "$schema": "https://extensions.shopifycdn.com/shopifycloud/schemas/v1/tool.json",
    "name": "publish_superapp_module",
    "description": "Open a Super App AI module on its page to publish its latest draft and take it live. The app runs plan-gating and pre-publish validation and the merchant confirms the publish (picking a theme for storefront modules). This action only navigates the merchant there — it never publishes automatically.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "moduleId": {
          "type": "string",
          "description": "The id of the module to publish (from a search_modules result)."
        }
      }
    }
  }
]
