{
  "$schema": "https://schemas.shopify.com/admin/intents/2026-04/intent.json",
  "inputSchema": {
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string",
        "description": "Natural-language description of the module to generate, e.g. 'a spin-to-win popup that matches my brand' or 'a free-shipping banner over $50'.",
        "minLength": 4,
        "mapTo": "query_param",
        "fieldName": "prompt"
      }
    }
  }
}
