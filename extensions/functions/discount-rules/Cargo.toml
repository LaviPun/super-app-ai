[package]
name = "discount-rules"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# In real Shopify Functions, use `shopify_function` crates provided by Shopify templates.
